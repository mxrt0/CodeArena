@model LeaderboardIndexViewModel
@{
    ViewData["Title"] = "Leaderboard";
}

<h1 class="mb-4 text-center">Leaderboard</h1>

<div class="table-responsive">
    <table class="table align-middle text-center">
        <thead>
            <tr>
                <th scope="col">Rank</th>
                <th scope="col">User</th>
                <th scope="col"><i class="bi bi-stars"></i> Total XP</th>
                <th scope="col"><i class="bi bi-trophy"></i> Challenges Solved</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var entry in Model.Leaderboard)
            {
                var isCurrentUser = entry.UserId == Model.CurrentUserId;
                var rowClass = isCurrentUser ? "table-info fw-bold" : "";

                <tr class="@rowClass">
                    <td>
                        @switch (entry.Rank)
                        {
                            case 1:
                                <span class="fs-5">🥇</span>
                                break;
                            case 2:
                                <span class="fs-5">🥈</span>
                                break;
                            case 3:
                                <span class="fs-5">🥉</span>
                                break;
                            default:
                                @entry.Rank
                                break;
                        }
                    </td>
                    <td>@entry.DisplayName</td>
                    <td>@entry.TotalXp</td>
                    <td>@entry.ChallengesSolved</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        const currentUserId = '@(Model.CurrentUserId)';
    </script>
    <script src="~/lib/microsoft/signalr/dist/browser/signalr.min.js"></script>
    <script src="~/js/leaderboard.js"></script>
 }